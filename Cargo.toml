[workspace]
members = [ "app", "app-derive" ]

exclude = [ "hfuzz", "tmp-vendor" ]

resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
lto = false
codegen-units = 1
debug = true
opt-level = "z"
